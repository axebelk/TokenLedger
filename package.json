{
  "name": "tokenledger",
  "private": true,
  "version": "0.1.0",
  "description": "TokenLedger — open-source AI cost governance and usage analytics platform",
  "license": "Apache-2.0",
  "type": "module",
  "packageManager": "pnpm@11.12.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "turbo dev",
    "dev:infra": "docker compose -f docker-compose.dev.yml up -d",
    "start:local": "node scripts/start-local.mjs",
    "build": "turbo build",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint",
    "test": "turbo test",
    "db:generate": "pnpm --filter @tokenledger/db generate",
    "db:migrate": "pnpm --filter @tokenledger/db migrate:dev",
    "db:deploy": "pnpm --filter @tokenledger/db migrate:deploy",
    "db:seed": "pnpm --filter @tokenledger/db seed",
    "db:studio": "pnpm --filter @tokenledger/db studio"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.8.0"
  }
}
